/**
 * @argobeat/engine — Version
 *
 * Single source of truth for the engine package version.
 * Updated on each release. Used by the public API and build tooling.
 */
export const VERSION = '2.0.0';
